"use client";

import axios from "axios";

const Authentication = () => {
  const handleLogOut = async () => {};
  return (
    <div className="text-white text-3xl" onClick={handleLogOut}>
      Protected page
    </div>
  );
};

export default Authentication;
